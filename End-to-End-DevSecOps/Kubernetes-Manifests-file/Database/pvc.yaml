apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-volume-claim
  namespace: three-tier
spec: 
  accessModes:  
    - ReadWriteOnce
  storageClassName: gp2  # Dynamic provisioning using EBS-backed storage
  resources:
    requests:
      storage: 1Gi
